"use client";

import React from "react";

interface ProductReviewsSectionProps {
  productId?: string;
  [key: string]: unknown;
}

/** Stub — product reviews section removed during kiosk migration. */
export default function ProductReviewsSection(_props: ProductReviewsSectionProps) {
  return null;
}
