"use client";

import React from "react";

interface WriteEditReviewDialogProps {
  children?: React.ReactNode;
  [key: string]: unknown;
}

/** Stub — write/edit review dialog removed during kiosk migration. */
export default function WriteEditReviewDialog({ children }: WriteEditReviewDialogProps) {
  return <>{children}</>;
}
