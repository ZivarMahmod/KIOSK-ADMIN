"use client";

import React from "react";

interface InvoiceDialogProps {
  children?: React.ReactNode;
}

/** Stub — invoice dialog removed during kiosk migration. */
export default function InvoiceDialog({ children }: InvoiceDialogProps) {
  return <>{children}</>;
}
