"use client";

import React from "react";

/** Stub — supplier list removed during kiosk migration. */
const SupplierList = React.memo(function SupplierList() {
  return null;
});

export default SupplierList;
